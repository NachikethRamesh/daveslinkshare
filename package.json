{
  "name": "daves-link-sharing-app",
  "version": "1.0.0",
  "description": "A secure link sharing application with user authentication",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "setup": "node server/setup.js",
    "setup:cloudflare": "node setup-cloudflare-secrets.js",
    "test-env": "node server/test-env.js",
    "build": "node build.js",
    "deploy": "npm run build",
    "deploy:pages": "npm run build",
    "deploy:worker": "wrangler deploy server/server.js",
    "test": "echo 'Tests not implemented yet'"
  },
  "keywords": [
    "link-sharing",
    "bookmark",
    "pocket-alternative",
    "nodejs",
    "express"
  ],
  "author": "Dave",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.5.0",
    "express-rate-limit": "^6.10.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
