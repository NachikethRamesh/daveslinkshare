{
  "name": "daves-link-sharing-app",
  "version": "1.0.0",
  "description": "A secure link sharing application with Cloudflare Pages Functions",
  "main": "client/index.html",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler pages deploy dist",
    "pages:deploy": "wrangler pages deploy dist"
  },
  "keywords": [
    "link-sharing",
    "bookmark",
    "pocket-alternative",
    "cloudflare-pages",
    "serverless"
  ],
  "author": "Dave",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
